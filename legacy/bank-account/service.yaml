apiVersion: v1
kind: Service
metadata:
  labels:
    app: bank-account
  name: bank-account
spec:
  ports:
    - name: http
      port: 9090
      protocol: TCP
      targetPort: 9090
  selector:
    app: bank-account