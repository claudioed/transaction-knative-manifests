apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    version: v1
    app: card-one
  name: card-one
spec:
  replicas: 1
  selector:
    matchLabels:
      app: card-one
  template:
    metadata:
      labels:
        app: card-one
        version: v1
    spec:
      containers:
        - image: claudioed/card-one
          imagePullPolicy: Always
          resources:
            requests:
              memory: "256Mi"
              cpu: "700m"
            limits:
              memory: "512Mi"
              cpu: "900m"
          livenessProbe:
            httpGet:
              path: /health/live
              port: 8080
              scheme: HTTP
            initialDelaySeconds: 4
          name: card-one
          ports:
            - containerPort: 8080
              name: http
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /health/ready
              port: 8080
              scheme: HTTP
            initialDelaySeconds: 4